
def prime_factors(n) {
    factors = []
    for i in 2...math.sqrt(n)+1 {
        while n % i == 0 {
            factors.append(i)
            n = n / i
        }
    }
    if n > 1 {
        factors.append(n)
    }
    return factors
}


def main() {
    input_str = "65 3 abc -350"
    nums = input_str.split(" ") -> int ?? 0
    factors = nums -> prime_factors
    factors_squared = factors -> (x) => { x * x}
    print(factors_squared)
}